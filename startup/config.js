const config = require("config");

module.exports = function() {
  // if (!config.get('privateKey')){
  //     console.log('privatekey has not been set');
  //     throw new Error('fatal error:privatekey has not been set');
  // }
};
